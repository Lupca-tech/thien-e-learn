  <div class='nested-fields'>
    <%= f.hidden_field :date, value: date %>
    <div class="field">
      <%= f.label "Môn" %>
      <%= f.select :subject_id, options_for_select(subject_for_select, f.object.id) %>
    </div>
    <%= link_to_remove_association "Xoá môn học.", f %>
  </div>